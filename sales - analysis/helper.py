def calculate_total(quantity, price):
    """Calculate the total price for a given quantity and price."""
    return quantity * price

def format_currency(amount):
    """Format a currency amount as a string."""
    return f"${amount:.2f}"        